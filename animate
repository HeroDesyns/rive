<!DOCTYPE html>
<html>
<head>
  <script src="https://unpkg.com/@rive-app/webgl"></script>
</head>
<body>
  <canvas id="riveCanvas" width="500" height="500"></canvas>

  <script>
    const rive = new Rive({
      src: "your-file.riv", // Replace with the URL of your Rive file
      canvas: document.getElementById("riveCanvas"),
      autoplay: true,
      stateMachines: ["State Machine 1"], // Replace with your actual state machine name
      onStateChange: (event) => {
        if (event.data.includes("showconfirm")) {
          // Send a message to the Wix page
          window.parent.postMessage("sliderComplete", "*");
        }
      }
    });
  </script>
</body>
</html>
